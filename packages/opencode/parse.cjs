const fs = require('fs'); const txt = fs.readFileSync('tsc_stdout.txt', 'utf8'); const lines = txt.split('\n'); const dirs = {}; lines.forEach(l => { const m = l.match(/^src\/([^/]+)\//); if (m) { dirs[m[1]] = (dirs[m[1]]||0)+1; } }); console.log(Object.entries(dirs).sort((a, b) => b[1] - a[1]).map(e => e[0] + ': ' + e[1]).join('\n')); fs.writeFileSync('harness_err.txt', lines.filter(l => l.startsWith('src/harness/')).join('\n'));
